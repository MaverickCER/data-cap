{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://maverickcer.github.io/data-cap/schema/capability-model.schema.json",
  "title": "data-cap Capability Model",
  "description": "Canonical, versioned projection of every discovered capability's fields and operations (see ADR 0050). Generated from src/build/inventory.ts's CapabilityInventory type -- never hand-authored.",
  "$ref": "#/definitions/CapabilityInventory",
  "definitions": {
    "CapabilityInventory": {
      "type": "object",
      "properties": {
        "schemaVersion": {
          "type": "number",
          "const": 3,
          "description": "Always `CAPABILITY_MODEL_SCHEMA_VERSION`."
        },
        "capabilities": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CapabilityNode"
          },
          "description": "Every discovered, linked capability."
        },
        "warnings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ParseWarning"
          },
          "description": "Parse/link warnings carried through from the discovery/linking pass that produced this inventory, each `file` root-relative like every other path this model publishes (OUT-01)."
        }
      },
      "required": [
        "schemaVersion",
        "capabilities",
        "warnings"
      ],
      "additionalProperties": false,
      "description": "The one authoritative model every report generator reads from -- see `buildInventory`. This type IS `data-cap`'s Capability Model (ADR 0050), not a separate thing that produces one: `CAPABILITY_MODEL_SCHEMA_VERSION` above versions this exact interface. ADR 0050 deliberately kept the `CapabilityInventory`/`buildInventory` names rather than introducing a `CapabilityModel`/`buildCapabilityModel` wrapper, since this type was already the intended public model before that ADR existed -- a rename would duplicate an already-correct, already-public shape for no gain. A canonical, versioned, JSON-serializable projection of every discovered capability's fields and operations, active or not."
    },
    "CapabilityNode": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "description": "Root-relative POSIX path of the file declaring this capability -- the one place an absolute discovery path is converted for publication (OUT-01, see `display-path.ts`). Every downstream model that carries a capability's `file` (Ownership, Finding, Dependency, Manifest Snapshot) copies this value rather than re-deriving it, so there is exactly one conversion point. Re-derive a real path with `absolutePathFrom(root, file)` when actual filesystem/import work needs one."
        },
        "exportName": {
          "type": "string",
          "description": "The binding name the `buildData`/`createData` result is exported as."
        },
        "kind": {
          "type": "string",
          "enum": [
            "buildData",
            "createData"
          ],
          "description": "`\"buildData\"` or `\"createData\"` -- which call form declared this capability."
        },
        "docs": {
          "$ref": "#/definitions/LooseCapabilityDocs",
          "description": "This capability's own declared documentation, if any -- the single path for `owner`/`sensitivity`/`purpose`/`legalBasis`/`dataResidency`/`auditRequired` (ADR 0057): a capability has nothing above it to resolve against, so a separate top-level \"resolved\" property would only ever duplicate `docs.X` verbatim."
        },
        "active": {
          "type": "boolean",
          "description": "Defaults to `true` when `docs.active` is absent."
        },
        "exclusiveGroup": {
          "type": "string",
          "description": "Resolved: `docs.exclusiveGroup`, if declared."
        },
        "declarationPosition": {
          "$ref": "#/definitions/SourcePosition",
          "description": "Position of this capability's own `buildData`/`createData` call site -- always available (ADR 0052)."
        },
        "fields": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/FieldNode"
          },
          "description": "Every declared field."
        },
        "getters": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OperationNode"
          },
          "description": "Every declared getter."
        },
        "mutators": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OperationNode"
          },
          "description": "Every declared mutator."
        },
        "subscriptions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/OperationNode"
          },
          "description": "Every declared subscription."
        }
      },
      "required": [
        "file",
        "exportName",
        "kind",
        "active",
        "declarationPosition",
        "fields",
        "getters",
        "mutators",
        "subscriptions"
      ],
      "additionalProperties": false,
      "description": "One `buildData`/`createData` capability, resolved from the inventory's shared model."
    },
    "LooseCapabilityDocs": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "fields": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/Loosen%3CFieldDocs%3E"
          }
        },
        "getters": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/Loosen%3COperationDocs%3E"
          }
        },
        "mutators": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/Loosen%3COperationDocs%3E"
          }
        },
        "subscriptions": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/Loosen%3COperationDocs%3E"
          }
        },
        "evidence": {
          "type": "object",
          "properties": {
            "fields": {
              "type": "object",
              "additionalProperties": {
                "$ref": "#/definitions/Loosen%3CEvidenceFieldDocs%3E"
              }
            }
          },
          "additionalProperties": false
        },
        "name": {
          "type": "string",
          "description": "Display name -- falls back to the export/binding name when omitted."
        },
        "description": {
          "type": "string",
          "description": "Why this capability exists / what it's for."
        },
        "owner": {
          "type": "string",
          "description": "Who's accountable for this capability. A field's own `owner` (see `FieldDocs`) overrides this for that field specifically."
        },
        "category": {
          "type": "string",
          "description": "Free-form classification -- never validated."
        },
        "exclusiveGroup": {
          "type": "string",
          "description": "Mutual-exclusivity group: more than one *active* capability sharing this value is a real conflict (only one implementation of a group should be live at once)."
        },
        "active": {
          "type": "boolean",
          "description": "Defaults to `true` when omitted. Gates `exclusiveGroup` conflict checks and manifest membership."
        },
        "sensitivity": {
          "type": "string",
          "description": "Data classification. Standard vocabulary: `\"public\"` / `\"internal\"` / `\"confidential\"` / `\"restricted\"` (ascending) -- custom values are allowed but flagged as non-standard, never blocking."
        },
        "protections": {
          "type": "string",
          "description": "Documented safeguard -- presence only, never an adequacy claim."
        },
        "retention": {
          "type": "string",
          "description": "Documented retention policy -- presence only, never an enforcement claim."
        },
        "purpose": {
          "type": "string",
          "description": "The declared reason this capability's data is collected/retained. Declared only -- data-cap never verifies the stated purpose matches actual usage. A field's own `purpose` (see `FieldDocs`) overrides this for that field specifically."
        },
        "legalBasis": {
          "type": "string",
          "description": "The declared legal basis asserted for processing this capability's data (e.g. `\"consent\"`, `\"contract\"`, `\"legitimate interest\"`). Records that a basis was declared -- never that data-cap has determined the basis is legally valid; data-cap records declared governance facts, it does not determine whether those facts satisfy a law. A field's own `legalBasis` overrides this for that field specifically."
        },
        "dataResidency": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "The declared jurisdiction(s) this capability's data is permitted/ expected to be *stored* in -- a policy constraint, not an observed fact, not a processing-location claim, not a data-subject-location claim. A field's own `dataResidency` overrides this for that field specifically."
        },
        "auditRequired": {
          "type": "boolean",
          "description": "Whether access to this capability's data is documented as requiring an audit trail. Declared only, same presence-only discipline as `protections`/`retention`. A field's own `auditRequired` overrides this for that field specifically."
        },
        "expiresAt": {
          "type": "string",
          "description": "When this capability's data (or the credential/source behind it) is declared to stop being valid, as an ISO-8601 date. A field's own `expiresAt` (see `FieldDocs`) overrides this for that field specifically. Declared only -- nothing expires at runtime."
        },
        "deprecated": {
          "type": "boolean",
          "description": "Whether this capability is documented as deprecated. Presence-only; nothing warns at runtime."
        },
        "deprecatedReason": {
          "type": "string",
          "description": "Why this capability is deprecated, and what to use instead. Only meaningful alongside `deprecated`."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {},
          "description": "Free-form extension bag for anything data-cap itself has no named concept for -- opaque, never inspected or validated by any code path in this package (e.g. jurisdiction-specific regulatory classification: `{ regulatory: \"GDPR,PCI-DSS\" }`). Once a concept matters enough for data-cap to reason about, it gets its own named field above (as `purpose`/`legalBasis`/`dataResidency`/`auditRequired` did); everything else stays here."
        }
      }
    },
    "Loosen<FieldDocs>": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "Why this field exists / what it's for."
        },
        "owner": {
          "type": "string",
          "description": "Overrides the capability's own `owner` for this field specifically."
        },
        "sensitivity": {
          "type": "string",
          "description": "Overrides the capability's own `sensitivity` for this field specifically."
        },
        "protections": {
          "type": "string",
          "description": "Overrides the capability's own `protections` for this field specifically."
        },
        "retention": {
          "type": "string",
          "description": "Documented retention policy for this field -- presence only, never an enforcement claim."
        },
        "purpose": {
          "type": "string",
          "description": "The declared reason this field's data is collected/retained. Declared only -- data-cap never verifies the stated purpose matches actual usage. Overrides the capability's own `purpose` for this field specifically."
        },
        "legalBasis": {
          "type": "string",
          "description": "The declared legal basis asserted for processing this field (e.g. `\"consent\"`, `\"contract\"`, `\"legitimate interest\"`). Records that a basis was declared -- never that data-cap has determined the basis is legally valid. Overrides the capability's own `legalBasis` for this field specifically."
        },
        "dataResidency": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "The declared jurisdiction(s) this field's data is permitted/expected to be *stored* in -- a policy constraint, not an observed fact, not a processing-location claim, not a data-subject-location claim. Overrides the capability's own `dataResidency` for this field specifically."
        },
        "auditRequired": {
          "type": "boolean",
          "description": "Whether access to this field is documented as requiring an audit trail. Declared only, same presence-only discipline as `protections`/ `retention`. Overrides the capability's own `auditRequired` for this field specifically."
        },
        "expiresAt": {
          "type": "string",
          "description": "When this field's data (or the credential/source behind it) is declared to stop being valid, as an ISO-8601 date (`\"2026-12-31\"`). Declared only: nothing expires anything at runtime, and `data-cap` never fetches a real expiry from a provider. Build tooling reads it to report what is expiring soon (see the Lifecycle Model) -- a date that has already passed is reported as expired, never acted on."
        },
        "deprecated": {
          "type": "boolean",
          "description": "Whether this field is documented as deprecated. Presence-only, same discipline as every other declared fact here -- nothing warns at runtime."
        },
        "deprecatedReason": {
          "type": "string",
          "description": "Why this field is deprecated, and what to use instead. Only meaningful alongside `deprecated`."
        },
        "removeBy": {
          "type": "string",
          "description": "The date by which a deprecated field is intended to be removed, ISO-8601. Field-level only: a removal deadline is about one specific field's own migration, and a capability-wide `removeBy` would say nothing about which of its fields still need work."
        },
        "renamedFrom": {
          "type": "string",
          "description": "The previous name of this field, when it was renamed. Field-level only, for the same reason as `removeBy`. Build tooling uses this to correlate an added-field/removed-field pair across two runs into one rename rather than reporting an unrelated addition and deletion -- see `ChangeModel`'s `renamedFields`. Only ever populated from an *authored* value, never guessed from name similarity."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {},
          "description": "Free-form extension bag for anything data-cap itself has no named concept for -- opaque, never inspected or validated by any code path in this package. Once a concept matters enough for data-cap to reason about, it gets its own named field above; everything else stays here."
        }
      },
      "additionalProperties": false,
      "description": "Every property here is always assigned (never conditionally omitted -- see extractCapabilityDocs()/extractOperationDocsMap()/ extractEvidenceFieldsMap() below), just sometimes with an `undefined` value when the AST literal didn't declare it. exactOptionalPropertyTypes distinguishes \"key absent\" from \"key present holding undefined\"; widening every property to explicitly include `| undefined` (rather than routing every assignment through a conditional-spread \"omit if absent\" instead) matches what this type actually represents: a raw, unconstrained parse result, not a validated CapabilityDocs."
    },
    "Loosen<OperationDocs>": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "What this operation does."
        },
        "source": {
          "type": "string",
          "description": "Short identifier for where this operation acquires/sends data (e.g. `\"stripe-api\"`, `\"postgres:users\"`), not prose."
        },
        "credentials": {
          "type": "string",
          "description": "What's required to call this operation -- descriptive only, never an authorization/enforcement claim."
        },
        "response": {
          "type": "string",
          "description": "Documented shape of what this operation returns/produces."
        },
        "endpoints": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DataFlowEndpoint"
          },
          "description": "Declared data-flow boundaries this operation crosses -- see `DataFlowEndpoint`."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {},
          "description": "Free-form extension bag for anything data-cap itself has no named concept for -- opaque, never inspected or validated by any code path in this package. The index signature below remains for the same reason it always has (documenting never fights the type checker); `metadata` is the recommended place for new extension data going forward."
        }
      },
      "additionalProperties": {
        "anyOf": [
          {},
          {
            "not": {}
          }
        ]
      },
      "description": "Every property here is always assigned (never conditionally omitted -- see extractCapabilityDocs()/extractOperationDocsMap()/ extractEvidenceFieldsMap() below), just sometimes with an `undefined` value when the AST literal didn't declare it. exactOptionalPropertyTypes distinguishes \"key absent\" from \"key present holding undefined\"; widening every property to explicitly include `| undefined` (rather than routing every assignment through a conditional-spread \"omit if absent\" instead) matches what this type actually represents: a raw, unconstrained parse result, not a validated CapabilityDocs."
    },
    "DataFlowEndpoint": {
      "type": "object",
      "properties": {
        "direction": {
          "$ref": "#/definitions/DataFlowDirection",
          "description": "Which way data moves across this boundary."
        },
        "kind": {
          "$ref": "#/definitions/DataFlowEndpointKind",
          "description": "What kind of boundary this is."
        },
        "name": {
          "type": "string",
          "description": "Short identifier, e.g. \"stripe-api\", \"postgres:users\" -- a semantic label, not a sentence."
        },
        "url": {
          "type": "string",
          "description": "A literal, trackable location for this endpoint -- a URL, a route path, a `table:column` reference. Distinct from `name` (a short label): this is meant to be compared/matched across capabilities (e.g. flagging two capabilities that declare the same URL as likely-duplicate fetches), not just read by a person."
        },
        "handling": {
          "type": "string",
          "enum": [
            "plaintext",
            "masked",
            "redacted",
            "hashed",
            "encrypted"
          ],
          "description": "The declared data-safety state of this field's value AT this specific boundary crossing -- e.g. a field that arrives `\"encrypted\"` on an `\"input\"` endpoint and is sent back out `\"redacted\"` on an `\"output\"` endpoint. Declared only, same presence-only discipline as every other field in this vocabulary: this records what a developer states about handling at this crossing, never that data-cap has verified the field's actual runtime value matches the claim."
        }
      },
      "required": [
        "direction",
        "kind",
        "name"
      ],
      "additionalProperties": false,
      "description": "One declared data-flow boundary an operation crosses. A getter/ subscription's endpoints are typically `\"input\"` (where `execute`/ `subscribe` acquires data from); a mutator's are typically `\"output\"` (where it sends data to) -- nothing enforces the typical case, since this is documentation, not a runtime contract."
    },
    "DataFlowDirection": {
      "type": "string",
      "enum": [
        "input",
        "output"
      ],
      "description": "Which direction data moves across a declared boundary, and what kind of boundary it is. Author-declared documentation only -- never statically verified against an operation's actual `execute`/`subscribe` body (those stay opaque per AGENTS.md invariant 6). A report may never present an endpoint as a proven fact; see AGENTS.md's declared-vs-proven invariant."
    },
    "DataFlowEndpointKind": {
      "type": "string",
      "enum": [
        "database",
        "cache",
        "storage",
        "queue",
        "api",
        "external-service",
        "user-input",
        "computed",
        "internal"
      ],
      "description": "What kind of boundary a declared data-flow endpoint crosses -- a data store (`database`/`cache`/`storage`/`queue`), an external entity (`api`/`external-service`/`user-input`), or something internal to this codebase (`computed`/`internal`)."
    },
    "Loosen<EvidenceFieldDocs>": {
      "type": "object",
      "properties": {
        "dynamicAccess": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Citations of where this field is accessed in a way static analysis can't see on its own (e.g. a truly dynamic key derived at runtime), each `\"<relative-path>:<line>:<column>\"`. Re-verified every run: a citation whose file no longer exists, or whose cited line has visibly changed since the citation was written, is downgraded and flagged, never trusted forever."
        }
      },
      "additionalProperties": false,
      "description": "Every property here is always assigned (never conditionally omitted -- see extractCapabilityDocs()/extractOperationDocsMap()/ extractEvidenceFieldsMap() below), just sometimes with an `undefined` value when the AST literal didn't declare it. exactOptionalPropertyTypes distinguishes \"key absent\" from \"key present holding undefined\"; widening every property to explicitly include `| undefined` (rather than routing every assignment through a conditional-spread \"omit if absent\" instead) matches what this type actually represents: a raw, unconstrained parse result, not a validated CapabilityDocs."
    },
    "SourcePosition": {
      "type": "object",
      "properties": {
        "line": {
          "type": "number"
        },
        "column": {
          "type": "number"
        }
      },
      "required": [
        "line",
        "column"
      ],
      "additionalProperties": false,
      "description": "A 1-based line/column within a file already known from context (e.g. `DependencyEdge.from`)."
    },
    "FieldNode": {
      "type": "object",
      "properties": {
        "path": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Top-level field key, as a single-element path (see the module doc comment on why granularity stops here)."
        },
        "docs": {
          "$ref": "#/definitions/FieldDocs",
          "description": "This field's own declared documentation, if any -- `description`/`protections`/`retention`/`metadata` have no resolution concept and live only here; for the six governed properties below, prefer `.value`/`.declaredOn` over reading this object directly (ADR 0057)."
        },
        "owner": {
          "$ref": "#/definitions/ResolvedGovernanceValue%3Cstring%3E",
          "description": "Resolved: the field's own `docs.owner`, else the capability's `owner`, plus which one declared it."
        },
        "sensitivity": {
          "$ref": "#/definitions/ResolvedGovernanceValue%3Cstring%3E",
          "description": "Resolved: the field's own `docs.sensitivity`, else the capability's `sensitivity` -- same inheritance rule as `owner` (ADR 0049, ADR 0050, ADR 0057)."
        },
        "purpose": {
          "$ref": "#/definitions/ResolvedGovernanceValue%3Cstring%3E",
          "description": "Resolved: the field's own `docs.purpose`, else the capability's `purpose` -- same inheritance rule as `owner`/`sensitivity` (ADR 0051, ADR 0057). Declared only, never verified."
        },
        "legalBasis": {
          "$ref": "#/definitions/ResolvedGovernanceValue%3Cstring%3E",
          "description": "Resolved: the field's own `docs.legalBasis`, else the capability's `legalBasis` -- same inheritance rule. A declared fact, never a legal determination (ADR 0051, ADR 0057)."
        },
        "dataResidency": {
          "$ref": "#/definitions/ResolvedGovernanceValue%3C(string%7Cstring%5B%5D)%3E",
          "description": "Resolved: the field's own `docs.dataResidency`, else the capability's `dataResidency` -- same inheritance rule. The declared permitted-storage jurisdiction(s), not an observed fact (ADR 0051, ADR 0057)."
        },
        "auditRequired": {
          "$ref": "#/definitions/ResolvedGovernanceValue%3Cboolean%3E",
          "description": "Resolved: the field's own `docs.auditRequired`, else the capability's `auditRequired` -- same inheritance rule (ADR 0051, ADR 0057)."
        },
        "writtenBy": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "getter",
                  "mutator",
                  "subscription"
                ],
                "description": "Which kind of operation wrote this field."
              },
              "name": {
                "type": "string",
                "description": "The writing operation's key name."
              }
            },
            "required": [
              "kind",
              "name"
            ],
            "additionalProperties": false
          },
          "description": "Every getter/mutator/subscription whose statically-extracted `writes` shape claims this field."
        },
        "shape": {
          "description": "The field's literal-evaluated declared default -- structural comparison only (e.g. `structural-duplication.ts`), never a runtime value."
        },
        "declarationPosition": {
          "$ref": "#/definitions/SourcePosition",
          "description": "Position of this field's own key in the `fields` object literal -- only when `fields` was resolved as an inline literal in this file; `undefined` when identifier-resolved across a file boundary this pass doesn't retain AST access to (never guessed at -- ADR 0052)."
        }
      },
      "required": [
        "path",
        "owner",
        "sensitivity",
        "purpose",
        "legalBasis",
        "dataResidency",
        "auditRequired",
        "writtenBy",
        "shape"
      ],
      "additionalProperties": false
    },
    "FieldDocs": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "Why this field exists / what it's for."
        },
        "owner": {
          "type": "string",
          "description": "Overrides the capability's own `owner` for this field specifically."
        },
        "sensitivity": {
          "type": "string",
          "description": "Overrides the capability's own `sensitivity` for this field specifically."
        },
        "protections": {
          "type": "string",
          "description": "Overrides the capability's own `protections` for this field specifically."
        },
        "retention": {
          "type": "string",
          "description": "Documented retention policy for this field -- presence only, never an enforcement claim."
        },
        "purpose": {
          "type": "string",
          "description": "The declared reason this field's data is collected/retained. Declared only -- data-cap never verifies the stated purpose matches actual usage. Overrides the capability's own `purpose` for this field specifically."
        },
        "legalBasis": {
          "type": "string",
          "description": "The declared legal basis asserted for processing this field (e.g. `\"consent\"`, `\"contract\"`, `\"legitimate interest\"`). Records that a basis was declared -- never that data-cap has determined the basis is legally valid. Overrides the capability's own `legalBasis` for this field specifically."
        },
        "dataResidency": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "The declared jurisdiction(s) this field's data is permitted/expected to be *stored* in -- a policy constraint, not an observed fact, not a processing-location claim, not a data-subject-location claim. Overrides the capability's own `dataResidency` for this field specifically."
        },
        "auditRequired": {
          "type": "boolean",
          "description": "Whether access to this field is documented as requiring an audit trail. Declared only, same presence-only discipline as `protections`/ `retention`. Overrides the capability's own `auditRequired` for this field specifically."
        },
        "expiresAt": {
          "type": "string",
          "description": "When this field's data (or the credential/source behind it) is declared to stop being valid, as an ISO-8601 date (`\"2026-12-31\"`). Declared only: nothing expires anything at runtime, and `data-cap` never fetches a real expiry from a provider. Build tooling reads it to report what is expiring soon (see the Lifecycle Model) -- a date that has already passed is reported as expired, never acted on."
        },
        "deprecated": {
          "type": "boolean",
          "description": "Whether this field is documented as deprecated. Presence-only, same discipline as every other declared fact here -- nothing warns at runtime."
        },
        "deprecatedReason": {
          "type": "string",
          "description": "Why this field is deprecated, and what to use instead. Only meaningful alongside `deprecated`."
        },
        "removeBy": {
          "type": "string",
          "description": "The date by which a deprecated field is intended to be removed, ISO-8601. Field-level only: a removal deadline is about one specific field's own migration, and a capability-wide `removeBy` would say nothing about which of its fields still need work."
        },
        "renamedFrom": {
          "type": "string",
          "description": "The previous name of this field, when it was renamed. Field-level only, for the same reason as `removeBy`. Build tooling uses this to correlate an added-field/removed-field pair across two runs into one rename rather than reporting an unrelated addition and deletion -- see `ChangeModel`'s `renamedFields`. Only ever populated from an *authored* value, never guessed from name similarity."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {},
          "description": "Free-form extension bag for anything data-cap itself has no named concept for -- opaque, never inspected or validated by any code path in this package. Once a concept matters enough for data-cap to reason about, it gets its own named field above; everything else stays here."
        }
      },
      "additionalProperties": false,
      "description": "Documentation metadata for one field.\n\n`owner`, `sensitivity`, and `protections` override the capability-level value of the same name for this field specifically -- not every field in a capability is equally sensitive or equally owned. `protections` and `retention` are documentation-presence signals only: a generator may say a safeguard/policy is \"documented\" or \"not documented,\" never that it is adequate, correct, or enforced. `purpose`/`legalBasis`/`dataResidency`/ `auditRequired` carry the same presence-only discipline -- declared governance facts, never a claim that data-cap has determined they satisfy any law (see `specs/decisions/0051-documentdata-metadata-and-governance-fields.md`).\n\nThe boundary this interface draws is deliberate: a named property here is reserved for a concept data-cap itself understands, projects, or reports on. Anything else -- organization-specific detail with no data-cap-defined meaning -- belongs in `metadata`, never as an ad hoc extra property, so this vocabulary stays bounded instead of accreting one-off keys over time.\n\nNote what is deliberately absent: there is no per-field `validate`/ `validator` property, and no `helpers.validators` combinator library backing one, unlike `@maverickcer/env-cap`'s `EnvVarDocs`. That is a design choice, not a gap. `env-cap` validates on a reject-and-throw pipeline, where a failing validator must carry a message explaining the rejection; `data-cap` has no reject path to write a message for -- invalid processor/operation output resets the field to its declared default via `core/schema.ts`'s `checkValueAgainstSchema`, keeping fields synchronously readable (AGENTS.md invariant 2) and surfacing the mismatch through `DataInfo`. See `helpers/shape.ts`'s own module comment for the full reasoning."
    },
    "ResolvedGovernanceValue<string>": {
      "type": "object",
      "properties": {
        "value": {
          "type": "string",
          "description": "The effective value: the field's own declaration, else the capability's."
        },
        "declaredOn": {
          "type": "string",
          "enum": [
            "field",
            "capability"
          ],
          "description": "Where `value` came from."
        }
      },
      "additionalProperties": false,
      "description": "One resolved governance value plus its provenance (ADR 0057) -- the one path a `FieldNode` consumer reads for a governed property, replacing what used to require separately checking a bare resolved property AND `field.docs.X` to get the full picture. `declaredOn: undefined` means neither the field nor its capability declared a value at all (`value` is then also `undefined`)."
    },
    "ResolvedGovernanceValue<(string|string[])>": {
      "type": "object",
      "properties": {
        "value": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "The effective value: the field's own declaration, else the capability's."
        },
        "declaredOn": {
          "type": "string",
          "enum": [
            "field",
            "capability"
          ],
          "description": "Where `value` came from."
        }
      },
      "additionalProperties": false,
      "description": "One resolved governance value plus its provenance (ADR 0057) -- the one path a `FieldNode` consumer reads for a governed property, replacing what used to require separately checking a bare resolved property AND `field.docs.X` to get the full picture. `declaredOn: undefined` means neither the field nor its capability declared a value at all (`value` is then also `undefined`)."
    },
    "ResolvedGovernanceValue<boolean>": {
      "type": "object",
      "properties": {
        "value": {
          "type": "boolean",
          "description": "The effective value: the field's own declaration, else the capability's."
        },
        "declaredOn": {
          "type": "string",
          "enum": [
            "field",
            "capability"
          ],
          "description": "Where `value` came from."
        }
      },
      "additionalProperties": false,
      "description": "One resolved governance value plus its provenance (ADR 0057) -- the one path a `FieldNode` consumer reads for a governed property, replacing what used to require separately checking a bare resolved property AND `field.docs.X` to get the full picture. `declaredOn: undefined` means neither the field nor its capability declared a value at all (`value` is then also `undefined`)."
    },
    "OperationNode": {
      "type": "object",
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "getter",
            "mutator",
            "subscription"
          ],
          "description": "Which kind of operation this is."
        },
        "name": {
          "type": "string",
          "description": "The operation's key name."
        },
        "docs": {
          "$ref": "#/definitions/OperationDocs",
          "description": "This operation's own declared documentation, if any."
        },
        "writes": {
          "type": "array",
          "items": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Top-level field paths this operation's statically-extracted `writes` shape covers. Empty when `writes` was absent (on a getter/subscription) or unresolvable -- never guessed at."
        },
        "hasProcessor": {
          "type": "boolean",
          "description": "Whether this operation's config object literal declares a `processor` key -- a proven, structural presence fact (never an evaluation of what the processor does; see ADR 0050)."
        },
        "hasOptimistic": {
          "type": "boolean",
          "description": "Whether this operation's config object literal declares an `optimistic` key -- structurally always `false` for a getter/subscription."
        },
        "endpoints": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DataFlowEndpoint"
          },
          "description": "Hoisted from `docs.endpoints` for convenient lookup -- author-declared, never statically verified."
        }
      },
      "required": [
        "kind",
        "name",
        "writes",
        "hasProcessor",
        "hasOptimistic",
        "endpoints"
      ],
      "additionalProperties": false,
      "description": "One getter/mutator/subscription operation, resolved from the inventory's shared model."
    },
    "OperationDocs": {
      "type": "object",
      "properties": {
        "description": {
          "type": "string",
          "description": "What this operation does."
        },
        "source": {
          "type": "string",
          "description": "Short identifier for where this operation acquires/sends data (e.g. `\"stripe-api\"`, `\"postgres:users\"`), not prose."
        },
        "credentials": {
          "type": "string",
          "description": "What's required to call this operation -- descriptive only, never an authorization/enforcement claim."
        },
        "response": {
          "type": "string",
          "description": "Documented shape of what this operation returns/produces."
        },
        "endpoints": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DataFlowEndpoint"
          },
          "description": "Declared data-flow boundaries this operation crosses -- see `DataFlowEndpoint`."
        },
        "metadata": {
          "type": "object",
          "additionalProperties": {},
          "description": "Free-form extension bag for anything data-cap itself has no named concept for -- opaque, never inspected or validated by any code path in this package. The index signature below remains for the same reason it always has (documenting never fights the type checker); `metadata` is the recommended place for new extension data going forward."
        }
      },
      "additionalProperties": {},
      "description": "Documentation metadata for one getter/mutator/subscription. Reserved keys are explicitly typed (mirroring the vocabulary real capabilities tend to document -- what it does, where it talks to, what it costs to call) while still accepting arbitrary extra keys for anything project-specific, so documenting never fights the type checker.\n\n`credentials` is descriptive only -- documenting `credentials: \"user session\"` is never proof that authorization is actually enforced. `endpoints` is likewise author-declared, never verified."
    },
    "ParseWarning": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "description": "Absolute path (or synthetic label, e.g. `\"(package) name\"`) the warning applies to."
        },
        "message": {
          "type": "string",
          "description": "Human-readable explanation of what was skipped and why."
        }
      },
      "required": [
        "file",
        "message"
      ],
      "additionalProperties": false,
      "description": "A recoverable issue found while statically parsing or linking one capability file -- never fatal, always surfaced to the caller as data."
    }
  }
}
