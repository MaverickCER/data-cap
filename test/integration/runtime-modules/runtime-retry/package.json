{
  "name": "data-cap-example-runtime-retry",
  "private": true,
  "type": "module",
  "description": "The optional opt-in runtime/retry, its abort-aware retry loop, and isStillDefault's guard against overwriting already-real data.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "data-cap": "file:../../../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
