{
  "name": "data-cap-example-optimistic-mutation-concurrency",
  "private": true,
  "type": "module",
  "description": "addPendingTransition/removePendingTransition: concurrent optimistic-UI mutations against unrelated fields, always folding onto current authoritative state, with no automatic rollback.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "data-cap": "file:../../../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
