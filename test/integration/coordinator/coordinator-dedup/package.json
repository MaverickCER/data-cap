{
  "name": "data-cap-example-coordinator-dedup",
  "private": true,
  "type": "module",
  "description": "Concurrent calls to the same function identity with canonically-equal params dedupe onto one in-flight promise; non-canonicalizable params degrade to no-dedup; a per-caller abort never cancels the shared underlying work.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@maverickcer/data-cap": "file:../../../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
