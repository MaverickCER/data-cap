{
  "name": "data-cap-example-subscription-lifecycle",
  "private": true,
  "type": "module",
  "description": "coordinator.acquireSubscription's ref-counted transport sharing: first subscriber connects, later ones reuse, only the last unsubscribe disconnects.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "data-cap": "file:../../../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
