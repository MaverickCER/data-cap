{
  "name": "data-cap-example-multi-capability-shared-transport",
  "private": true,
  "type": "module",
  "description": "Two different capabilities sharing one transport connection (same subscribe identity) while keeping fully independent state and processing.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "data-cap": "file:../../../.."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
