{
  "name": "data-cap-example-socket-io-subscription",
  "private": true,
  "type": "module",
  "description": "Wiring a real Socket.IO client into coordinator.acquireSubscription -- an adoption pattern, not a shipped adapter. Runs a real local Socket.IO server + client pair, no external network dependency.",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "data-cap": "file:../../../..",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
